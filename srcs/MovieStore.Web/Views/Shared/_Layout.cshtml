<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Sistema de Locadora</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.0/font/bootstrap-icons.css" rel="stylesheet">
    <style>
        body { background: linear-gradient(135deg, #f8f9fa 0%, #e9ecef 100%); }
        .navbar { background: linear-gradient(135deg, #495057 0%, #343a40 100%) !important; }
        .card { border: none; box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1); margin-bottom: 2rem; }
        .card-header { background: linear-gradient(135deg, #6c757d 0%, #495057 100%); color: white; font-weight: 600; }
        .table th { background-color: #f8f9fa; color: #495057; font-weight: 600; }
        .table-striped > tbody > tr:nth-of-type(odd) > td { background-color: rgba(0, 0, 0, 0.02); }
        .btn-add { background: linear-gradient(135deg, #28a745 0%, #20c997 100%); border: none; }
        .btn-add:hover { background: linear-gradient(135deg, #20c997 0%, #28a745 100%); transform: translateY(-1px); }
        .form-control { border: 1px solid #ced4da; }
        .form-control:focus { border-color: #20c997; box-shadow: 0 0 0 0.2rem rgba(32, 201, 151, 0.25); }
        .badge-count { background: linear-gradient(135deg, #17a2b8 0%, #20c997 100%); }
        .add-form { background: linear-gradient(135deg, #f8f9fa 0%, #ffffff 100%); border-top: 2px solid #dee2e6; }
        .icon-card { color: #6c757d; }
    </style>
</head>
<body>
    <nav class="navbar navbar-dark mb-4">
        <div class="container">
            <span class="navbar-brand mb-0 h1">
                <i class="bi bi-film me-2"></i>Sistema de Locadora
            </span>
        </div>
    </nav>
    
    <div class="container">
        @RenderBody()
    </div>
    
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>